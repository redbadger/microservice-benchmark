apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-go
spec:
  template:
    spec:
      containers:
        - name: api-go
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "250m"
