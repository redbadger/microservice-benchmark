apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-java-blocking
spec:
  template:
    spec:
      containers:
        - name: api-java-blocking
          resources:
            requests:
              memory: "256Mi"
              cpu: "2"
            limits:
              memory: "256Mi"
              cpu: "2"
