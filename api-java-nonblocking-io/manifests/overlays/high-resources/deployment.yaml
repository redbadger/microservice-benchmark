apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-java-nonblocking
spec:
  template:
    spec:
      containers:
        - name: api-java-nonblocking
          resources:
            requests:
              memory: "2Gi"
              cpu: "2"
            limits:
              memory: "2Gi"
              cpu: "2"
