apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-java-nonblocking
spec:
  template:
    spec:
      containers:
        - name: api-java-nonblocking
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "250m"
