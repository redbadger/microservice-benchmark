apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-java-nonblocking
spec:
  template:
    spec:
      containers:
        - name: api-java-nonblocking
          resources:
            requests:
              memory: "512Mi"
              cpu: "1"
            limits:
              memory: "512Mi"
              cpu: "1"
