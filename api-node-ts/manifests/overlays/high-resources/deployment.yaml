apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-node
spec:
  template:
    spec:
      containers:
        - name: api-node
          resources:
            requests:
              memory: "2Gi"
              cpu: "2"
            limits:
              memory: "2Gi"
              cpu: "2"
