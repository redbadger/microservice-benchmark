apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-node
spec:
  template:
    spec:
      containers:
        - name: api-node
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "250m"
