apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-node
spec:
  template:
    spec:
      containers:
        - name: api-node
          resources:
            requests:
              memory: "512Mi"
              cpu: "1"
            limits:
              memory: "512Mi"
              cpu: "1"
