apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: api-rust-tide
spec:
  hosts:
    - "*"
  gateways:
    - istio-system/ingressgateway
  http:
    - match:
        - uri:
            prefix: "/api-rust-tide/"
        - uri:
            prefix: "/api-rust-tide"
      rewrite:
        uri: "/"
      route:
        - destination:
            port:
              number: 80
            host: api-rust-tide
