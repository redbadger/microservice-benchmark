apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-rust
spec:
  template:
    spec:
      containers:
        - name: api-rust
          resources:
            requests:
              memory: "2Gi"
              cpu: "2"
            limits:
              memory: "2Gi"
              cpu: "2"
